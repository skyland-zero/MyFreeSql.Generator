using System;
using Student.Model.DTO.Aws;

namespace {{ Namespace }}.DTO
{
    /// <summary>
    /// {{ TableComment }}
    /// </summary>
    public class {{ TableCsName }}Dto{{ DtoExtends }}
    {
    {%- for col in Columns %}
        /// <summary>
        /// {{ col.Comment }}
        /// </summary>
        public {{ col.CsTypeName }} {{ col.CsName }} { get; set; }
    {% endfor %}
    }
}