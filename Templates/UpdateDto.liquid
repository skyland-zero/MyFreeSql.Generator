using System;

namespace {{ Namespace }}.DTO
{
    /// <summary>
    /// {{ TableComment }}
    /// </summary>
    public class {{ TableCsName }}UpdateDto
    {
    {%- for col in Columns %}
        /// <summary>
        /// {{ col.Comment }}
        /// </summary>
        public {{ col.CsTypeName }} {{ col.CsName }} { get; set; }
    {% endfor %}
    }
}